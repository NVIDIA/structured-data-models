<!--
Delete this comment. Leave the body empty when the title and the diff already say everything; the squash commit then falls back to your branch commit messages.
PRs here are squash-merged, so anything left in this box becomes the commit message body verbatim.

Otherwise tell the reviewer what the diff cannot, and drop every line that does not apply:

- What is wrong or missing today, or what goes wrong without this change.
- What a caller now has to do differently, and what is deliberately out of scope.
- Measured numbers, when the change claims a speed, memory, or accuracy effect.
- `Stacked on #N`, when this depends on another PR.

Length should track the decision a reviewer has to make, not the size of the diff. Do not narrate the diff, do not enumerate files, commits, or symbols, and do not add headings or sections.
Process and AI policy: CONTRIBUTING.md and AGENTS.md.
-->
